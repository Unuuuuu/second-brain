---
date: 2026-01-06
---

# 스토어 도메인 모델 정의

## 목표

스토어 도메인에 존재하는 도메인 모델들을 명확하게 정의한다.

## 설계 원칙

- **순수 도메인 모델**: 각 모델은 자기 고유 속성만 정의
- **관계는 조합으로 처리**: 다른 모델과의 관계는 사용처에서 조합

```typescript
// 사용 예시
interface ProductDetail {
    product: Product;
    categories: Category[];
    partner: Partner;
}
```

### 새 모델 추가 시 확인

- [ ] 자기 고유 속성만 정의했는가?
- [ ] 다른 모델 배열을 직접 포함하지 않았는가?
- [ ] 관계는 사용 예시로 보여주었는가?

## 대상 모델

### 상품 관련

- [x] Product

```typescript
interface Product {
    // ─────────────────────────────────────
    // 식별/기본
    // ─────────────────────────────────────
    productNo: number; // 상품번호 (PK)
    productName: string; // 상품명
    imageUrls: string[]; // 상품 이미지 URL
    content: string; // 상품설명 HTML
    dutyInfo: string; // 상품정보고시 (JSON)

    // ─────────────────────────────────────
    // 가격
    // ─────────────────────────────────────
    salePrice: number; // 상품판매가
    immediateDiscountAmt: number; // 즉시할인가
    couponDiscountAmt: number; // 쿠폰할인가

    // ─────────────────────────────────────
    // 재고/상태
    // ─────────────────────────────────────
    stockCnt: number; // 남은 수량
    displayableStock: boolean; // 재고 노출 여부
    display: boolean; // 전시여부
    soldout: boolean; // 품절여부
    saleStatusType: SaleStatusType; // 판매상태

    // ─────────────────────────────────────
    // 배송
    // ─────────────────────────────────────
    deliveryConditionType: DeliveryConditionType; // 배송비타입
    deliveryAmt?: number; // 배송비
    deliveryPrePayment: boolean; // 선/착불
    returnDeliveryAmt?: number; // 반품배송비
    remoteDeliveryAreaFees: RemoteDeliveryAreaFee[]; // 지역별 추가 배송비

    // ─────────────────────────────────────
    // 구매 제한
    // ─────────────────────────────────────
    minBuyCnt: number; // 최소구매수량
    maxBuyPersonCnt: number; // 1인최대구매수량

    // ─────────────────────────────────────
    // 카운터/평점
    // ─────────────────────────────────────
    likeCnt: number; // 좋아요 수
    reviewCnt: number; // 상품평 수
    reviewRate: number; // 상품평 평균점
    liked: boolean; // 좋아요 여부
}

enum SaleStatusType {
    ONSALE = "ONSALE",
    STOP = "STOP",
    PROHIBITION = "PROHIBITION",
}

enum DeliveryConditionType {
    FREE = "FREE",
    FIXED_FEE = "FIXED_FEE",
    CONDITIONAL = "CONDITIONAL",
}
```

- [x] ProductOption

```typescript
interface ProductOption {
    // ─────────────────────────────────────
    // 식별/기본
    // ─────────────────────────────────────
    optionNo: number; // 옵션번호 (PK)
    label: string; // 옵션명 (색상, 사이즈)
    value: string; // 옵션값 (빨강, M)
    isRequiredOption: boolean; // 필수 옵션 여부

    // ─────────────────────────────────────
    // 가격
    // ─────────────────────────────────────
    addPrice: number; // 추가금액
    buyPrice: number; // 할인적용가

    // ─────────────────────────────────────
    // 재고/상태
    // ─────────────────────────────────────
    stockCnt: number; // 재고수량
    saleType: OptionSaleType; // 판매타입
    main: boolean; // 대표 옵션 여부
    forcedSoldOut: boolean; // 임시 품절 여부

    // ─────────────────────────────────────
    // 이미지
    // ─────────────────────────────────────
    images: OptionImage[]; // 옵션 이미지
}

interface OptionImage {
    url: string; // 이미지 URL
    main: boolean; // 메인 이미지 여부
}

enum OptionSaleType {
    AVAILABLE = "AVAILABLE",
    SOLD_OUT = "SOLD_OUT",
}
```

### 주문 관련

- [x] OrderSheet

```typescript
// 사용 예시
interface OrderSheetDetail {
    orderSheet: OrderSheet;
    shippingAddress: ShippingAddress | null;
    deliveryGroups: Array<{
        group: OrderSheetDeliveryGroup;
        options: OrderSheetOption[];
    }>;
}

interface OrderSheet {
    // ─────────────────────────────────────
    // 결제 금액
    // ─────────────────────────────────────
    productAmt: number; // 상품금액
    totalDiscountAmt: number; // 총 할인금액
    deliveryAmt: number; // 배송비
    paymentAmt: number; // 결제예정금액
}

interface OrderSheetDeliveryGroup {
    partnerNo: number; // 파트너번호
    partnerName: string; // 파트너명
    deliveryAmt: number; // 배송비
}

interface ShippingAddress {
    // ─────────────────────────────────────
    // 식별
    // ─────────────────────────────────────
    addressNo: number | null; // 배송지 번호 (신규: null)
    addressName: string | null; // 주소록명
    isDefault: boolean; // 기본 배송지 여부

    // ─────────────────────────────────────
    // 수령자 정보
    // ─────────────────────────────────────
    receiverName: string; // 수령자명
    receiverContact1: string; // 연락처1
    receiverContact2: string | null; // 연락처2 (추가 연락처)

    // ─────────────────────────────────────
    // 주소
    // ─────────────────────────────────────
    receiverZipCd: string; // 우편번호
    receiverAddress: string; // 도로명 주소
    receiverDetailAddress: string; // 상세 주소

    // ─────────────────────────────────────
    // 배송 옵션
    // ─────────────────────────────────────
    courierRestriction: CourierRestriction; // 택배 제한 타입
    addressMemo: string | null; // 배송 메모
}

enum CourierRestriction {
    POST_OFFICE_ONLY = "POST_OFFICE_ONLY", // 우체국 택배만 배송 가능
    ALL = "ALL", // 모든 택배 배송 가능
}

interface OrderSheetOption {
    // ─────────────────────────────────────
    // 상품 정보
    // ─────────────────────────────────────
    productNo: number; // 상품번호
    productName: string; // 상품명
    imageUrl: string; // 상품 이미지 URL

    // ─────────────────────────────────────
    // 옵션 정보
    // ─────────────────────────────────────
    optionNo: number; // 옵션번호
    optionTitle: string; // 옵션 표시값 ("사이즈: FREE/색상: 검정")
    orderCnt: number; // 주문수량
    salePrice: number; // 판매가
    buyAmt: number; // 구매금액 (구매가 * 수량)
    soldOut: boolean; // 품절여부
}

enum PayType {
    CREDIT_CARD = "CREDIT_CARD", // 신용카드
    VIRTUAL_ACCOUNT = "VIRTUAL_ACCOUNT", // 가상계좌
    ACCOUNT = "ACCOUNT", // 계좌이체
    MOBILE = "MOBILE", // 휴대폰결제
    NAVER_PAY = "NAVER_PAY", // 네이버페이
    KAKAO_PAY = "KAKAO_PAY", // 카카오페이
    TOSS_PAY = "TOSS_PAY", // 토스페이
}
```

- [x] Order

```typescript
// 사용 예시
interface OrderDetail {
    order: Order;
    shippingAddress: ShippingAddress;
    receiptInfos: ReceiptInfo[];
    deliveryGroups: Array<{
        group: OrderDeliveryGroup;
        options: OrderOption[];
    }>;
}

interface OrderDeliveryGroup {
    // ─────────────────────────────────────
    // 파트너
    // ─────────────────────────────────────
    partnerNo: number; // 파트너번호
    partnerName: string; // 파트너명

    // ─────────────────────────────────────
    // 배송비
    // ─────────────────────────────────────
    deliveryAmt: number; // 배송비

    // ─────────────────────────────────────
    // 배송 추적
    // ─────────────────────────────────────
    invoiceNo: string | null; // 송장번호
    deliveryCompanyType: string | null; // 택배사 코드
    deliveryCompanyTypeLabel: string | null; // 택배사명 ("CJ대한통운")
}

interface Order {
    // ─────────────────────────────────────
    // 식별/기본
    // ─────────────────────────────────────
    orderNo: string; // 주문번호 (PK)
    orderYmdt: string; // 주문일시

    // ─────────────────────────────────────
    // 금액
    // ─────────────────────────────────────
    productAmt: number; // 상품 금액
    discountAmt: number; // 할인 금액
    deliveryAmt: number; // 배송비
    payAmt: number; // 결제 금액

    // ─────────────────────────────────────
    // 결제
    // ─────────────────────────────────────
    payType: PayType; // 결제수단
    payTypeLabel: string; // 결제수단 표시명 ("BC카드(일시불)")

    // ─────────────────────────────────────
    // 다음 액션
    // ─────────────────────────────────────
    nextActions: NextActionType[]; // 가능한 작업 (배송지변경 등)
}
```

- [x] OrderOption

```typescript
interface OrderOption {
    // ─────────────────────────────────────
    // 식별
    // ─────────────────────────────────────
    orderOptionNo: number; // 주문옵션번호 (PK)
    productNo: number; // 상품번호 (FK)
    optionNo: number; // 옵션번호 (FK)

    // ─────────────────────────────────────
    // 상품 정보
    // ─────────────────────────────────────
    productName: string; // 상품명
    imageUrl: string; // 상품 이미지
    optionTitle: string; // 옵션 표시값 ("색상/레드")

    // ─────────────────────────────────────
    // 주문 정보
    // ─────────────────────────────────────
    orderCnt: number; // 주문수량
    orderStatusType: OrderStatusType; // 주문상태
    orderStatusTypeLabel: string; // 주문상태 표시명 ("배송중")

    // ─────────────────────────────────────
    // 가격
    // ─────────────────────────────────────
    salePrice: number; // 판매가
    buyPrice: number; // 구매가 (할인 적용)
    buyAmt: number; // 구매금액 (구매가 * 수량)

    // ─────────────────────────────────────
    // 클레임 상태
    // ─────────────────────────────────────
    claimNo: number | null; // 클레임번호
    claimStatusType: ClaimStatusType | null; // 클레임상태

    // ─────────────────────────────────────
    // 다음 액션
    // ─────────────────────────────────────
    nextActions: NextActionType[]; // 가능한 작업 (취소/반품/교환/배송조회 등)
}

enum OrderStatusType {
    DEPOSIT_WAIT = "DEPOSIT_WAIT", // 입금대기
    PAY_DONE = "PAY_DONE", // 결제완료
    PRODUCT_PREPARE = "PRODUCT_PREPARE", // 상품준비중
    DELIVERY_PREPARE = "DELIVERY_PREPARE", // 배송준비중
    DELIVERY_ING = "DELIVERY_ING", // 배송중
    DELIVERY_DONE = "DELIVERY_DONE", // 배송완료
    BUY_CONFIRM = "BUY_CONFIRM", // 구매확정
    CANCEL_DONE = "CANCEL_DONE", // 취소완료
    RETURN_DONE = "RETURN_DONE", // 반품완료
    EXCHANGE_DONE = "EXCHANGE_DONE", // 교환완료
}

enum NextActionType {
    CANCEL_ALL = "CANCEL_ALL", // 주문취소 (전체)
    CANCEL = "CANCEL", // 취소
    EXCHANGE = "EXCHANGE", // 교환
    RETURN = "RETURN", // 반품
    WITHDRAW_CANCEL = "WITHDRAW_CANCEL", // 취소신청 철회
    WITHDRAW_EXCHANGE = "WITHDRAW_EXCHANGE", // 교환신청 철회
    WITHDRAW_RETURN = "WITHDRAW_RETURN", // 반품신청 철회
    VIEW_CLAIM = "VIEW_CLAIM", // 클레임 조회
    VIEW_DELIVERY = "VIEW_DELIVERY", // 배송조회
    DELIVERY_DONE = "DELIVERY_DONE", // 수취확인
    CONFIRM_ORDER = "CONFIRM_ORDER", // 구매확정
    CHANGE_ADDRESS = "CHANGE_ADDRESS", // 배송지변경
    WRITE_REVIEW = "WRITE_REVIEW", // 상품평작성
}
```

### 클레임 관련

- [x] Claim

```typescript
// 사용 예시
interface OrderDetailWithClaims {
    order: Order;
    shippingAddress: ShippingAddress;
    claims: Claim[];
}

// 클레임 상세 화면용
interface ClaimDetail {
    claim: Claim;
    claimedOptions: ClaimOption[];
}

interface Claim {
    // ─────────────────────────────────────
    // 식별/기본
    // ─────────────────────────────────────
    claimNo: number; // 클레임번호 (PK)
    orderNo: string; // 주문번호 (FK)
    claimType: ClaimType; // 취소/반품/교환
    claimReasonType: ClaimReasonType; // 사유 타입
    claimReasonDetail: string | null; // 사유 상세
    claimStatusType: ClaimStatusType; // 상태
    claimYmdt: string; // 신청일시
    claimCompleteYmdt: string | null; // 완료일시

    // ─────────────────────────────────────
    // 환불 금액
    // ─────────────────────────────────────
    refundPayAmt: number; // 환불금액 (적립금 포함)
    refundMainPayAmt: number; // 환불금액 (적립금 제외)
    refundSubPayAmt: number; // 적립금 환불금액
    productAmt: number; // 상품 금액
    discountAmt: number; // 할인 금액
    deliveryAmt: number; // 배송비
    refundDeliveryAmt: number; // 환불 배송비

    // ─────────────────────────────────────
    // 환불 방법
    // ─────────────────────────────────────
    refundPayType: PayType; // 환불 결제방법
    refundTypeLabel: string; // 환불방법 (노출용)
    refundBankAccount: BankAccount | null; // 환불계좌 (무통장/가상계좌)
}

enum ClaimType {
    CANCEL = "CANCEL", // 취소
    RETURN = "RETURN", // 반품
    EXCHANGE = "EXCHANGE", // 교환
}

enum ClaimReasonType {
    CHANGE_MIND = "CHANGE_MIND", // 단순변심
    DEFECTIVE = "DEFECTIVE", // 상품불량/파손
    WRONG_DELIVERY = "WRONG_DELIVERY", // 오배송
    WRONG_OPTION = "WRONG_OPTION", // 옵션 잘못 선택
    DELAYED_DELIVERY = "DELAYED_DELIVERY", // 배송지연
    OUT_OF_STOCK = "OUT_OF_STOCK", // 상품품절
    SERVICE_UNSATISFIED = "SERVICE_UNSATISFIED", // 서비스 불만족
    SOLD_OUT = "SOLD_OUT", // 판매자 품절
    OTHERS_SELLER = "OTHERS_SELLER", // 기타(판매자귀책)
    OTHERS_BUYER = "OTHERS_BUYER", // 기타(구매자귀책)
}

enum ClaimStatusType {
    // 취소
    CANCEL_REQUEST = "CANCEL_REQUEST", // 취소신청
    CANCEL_PROC_REQUEST_REFUND = "CANCEL_PROC_REQUEST_REFUND", // 취소처리중(환불요청)
    CANCEL_PROC_WAITING_REFUND = "CANCEL_PROC_WAITING_REFUND", // 취소처리중(환불대기)
    CANCEL_NO_REFUND = "CANCEL_NO_REFUND", // 취소완료(환불없음)
    CANCEL_DONE = "CANCEL_DONE", // 취소완료

    // 반품
    RETURN_REQUEST = "RETURN_REQUEST", // 반품신청
    RETURN_REJECT_REQUEST = "RETURN_REJECT_REQUEST", // 반품거부요청
    RETURN_REJECTED = "RETURN_REJECTED", // 반품거부
    RETURN_PROC_BEFORE_RECEIVE = "RETURN_PROC_BEFORE_RECEIVE", // 반품처리중(수거전)
    RETURN_PROC_REQUEST_REFUND = "RETURN_PROC_REQUEST_REFUND", // 반품처리중(환불요청)
    RETURN_PROC_WAITING_REFUND = "RETURN_PROC_WAITING_REFUND", // 반품처리중(환불대기)
    RETURN_NO_REFUND = "RETURN_NO_REFUND", // 반품완료(환불없음)
    RETURN_DONE = "RETURN_DONE", // 반품완료

    // 교환
    EXCHANGE_REQUEST = "EXCHANGE_REQUEST", // 교환신청
    EXCHANGE_REJECT_REQUEST = "EXCHANGE_REJECT_REQUEST", // 교환거부요청
    EXCHANGE_REJECTED = "EXCHANGE_REJECTED", // 교환거부
    EXCHANGE_PROC_BEFORE_RECEIVE = "EXCHANGE_PROC_BEFORE_RECEIVE", // 교환처리중(수거전)
    EXCHANGE_PROC_REQUEST_REFUND = "EXCHANGE_PROC_REQUEST_REFUND", // 교환처리중(환불요청)
    EXCHANGE_PROC_WAITING = "EXCHANGE_PROC_WAITING", // 교환처리중(교환대기)
    EXCHANGE_DONE = "EXCHANGE_DONE", // 교환완료
}
```

- [x] ClaimOption

```typescript
interface ClaimOption {
    // ─────────────────────────────────────
    // 식별
    // ─────────────────────────────────────
    orderOptionNo: number; // 주문옵션번호 (FK)
    productNo: number; // 상품번호 (FK)
    optionNo: number; // 옵션번호 (FK)

    // ─────────────────────────────────────
    // 상품 정보
    // ─────────────────────────────────────
    productName: string; // 상품명
    imageUrl: string; // 상품 이미지
    optionUsed: boolean; // 옵션 사용 여부
    optionName: string; // 옵션명 ("사이즈")
    optionValue: string; // 옵션값 ("M")
    optionTitle: string; // 옵션 표시값 ("사이즈/M")

    // ─────────────────────────────────────
    // 주문 정보
    // ─────────────────────────────────────
    orderCnt: number; // 주문수량

    // ─────────────────────────────────────
    // 가격
    // ─────────────────────────────────────
    buyAmt: number; // 구매금액 (구매가 * 수량)
}
```

### 가격/환불/영수증 관련

- [x] ReceiptInfo

```typescript
interface ReceiptInfo {
    receiptType: ReceiptType; // 영수증 타입
    url: string; // 영수증 URL (웹뷰)
}

enum ReceiptType {
    SALE_STATEMENT = "SALE_STATEMENT", // 매출전표
    CASH_RECEIPT = "CASH_RECEIPT", // 현금영수증
}
```

- [x] BankAccount

```typescript
interface BankAccount {
    bank: string; // 은행 코드
    bankName: string; // 은행명
    bankAccount: string | null; // 계좌번호
    bankDepositorName: string | null; // 예금주명
}
```

- ~~RefundInfo~~ → Claim에 통합

### 파트너 관련

- [ ] Partner

### 장바구니 관련

#### 장바구니 계층 구조

장바구니 API는 다음과 같은 4단계 계층 구조를 가집니다:

```
Cart (장바구니)
└── DeliveryGroup (배송 그룹) - 파트너/배송템플릿 기준
    └── OrderProduct (주문 상품) - 상품 1개
        └── OrderProductOption (주문 상품 옵션) - 장바구니에 담은 1건
            └── SetOption[] (세트 옵션) - 조합된 개별 옵션들
```

**실제 예시: 티셔츠 구매**

```
🛒 장바구니
└── 📦 배송그룹: "NCP 파트너"
    └── 👕 주문상품: "나이키 드라이핏 티셔츠"
        ├── 🏷️ 주문상품옵션 #1 (cartNo: 101)
        │   ├── optionTitle: "L / 블랙"
        │   ├── orderCnt: 2
        │   └── setOptions:
        │       ├── { optionName: "사이즈", optionValue: "L" }
        │       └── { optionName: "색상", optionValue: "블랙" }
        │
        └── 🏷️ 주문상품옵션 #2 (cartNo: 102)
            ├── optionTitle: "M / 화이트"
            ├── orderCnt: 1
            └── setOptions:
                ├── { optionName: "사이즈", optionValue: "M" }
                └── { optionName: "색상", optionValue: "화이트" }
```

**각 레벨의 역할**

| 레벨 | 모델 | 의미 | 예시 |
|------|------|------|------|
| 1 | CartDeliveryGroup | 배송 그룹 | "NCP 파트너 배송" |
| 2 | CartProduct | 상품 자체 | "나이키 드라이핏 티셔츠" |
| 3 | CartOption | 장바구니 담기 1건 | "L/블랙 2개" ← cartNo 1개 |
| 4 | CartSetOption[] | 옵션 조합의 구성요소 | 사이즈=L, 색상=블랙 |

**옵션 선택 방식 (selectType)**

- `FLAT` (단독형): setOptions에 1개만 존재
- `COMBINATION` (조합형): setOptions에 여러 개 존재

- [x] CartPrice

```typescript
interface CartPrice {
    // ─────────────────────────────────────
    // 금액
    // ─────────────────────────────────────
    buyAmt: number; // 구매금액 합
    discountAmt: number; // 할인금액
    totalDeliveryAmt: number; // 총 배송비
    totalAmt: number; // 총 결제금액
}
```

- [x] CartDeliveryGroup

```typescript
// 사용 예시
interface CartDetail {
    price: CartPrice;
    deliveryGroups: CartDeliveryGroupWithProducts[];
    invalidProducts: CartProductWithOptions[];
}

interface CartDeliveryGroupWithProducts {
    deliveryGroup: CartDeliveryGroup;
    products: CartProductWithOptions[];
}

interface CartProductWithOptions {
    product: CartProduct;
    options: CartOption[];
}

interface CartDeliveryGroup {
    // ─────────────────────────────────────
    // 파트너
    // ─────────────────────────────────────
    partnerNo: number | null; // 파트너번호
    partnerName: string; // 파트너명

    // ─────────────────────────────────────
    // 배송 템플릿
    // ─────────────────────────────────────
    deliveryTemplateGroupNo: number | null; // 템플릿 그룹 번호
    deliveryTemplateGroupName: string | null; // 템플릿 그룹명

    // ─────────────────────────────────────
    // 배송비
    // ─────────────────────────────────────
    buyAmt: number; // 그룹별 구매금액
    deliveryAmt: number; // 배송비
    deliveryPayType: DeliveryPayType; // 선불/착불

    // ─────────────────────────────────────
    // 배송 조건
    // ─────────────────────────────────────
    deliveryConditionType: DeliveryConditionType | null; // 배송조건타입
    aboveDeliveryAmt: number; // 무료배송 기준금액
    baseDeliveryAmt: number; // 기본 배송비
    remoteDeliveryAmt: number; // 도서산간 추가비
    returnDeliveryAmt: number; // 반품배송비
}

enum DeliveryPayType {
    PREPAID_DELIVERY = "PREPAID_DELIVERY", // 선불
    PAY_ON_DELIVERY = "PAY_ON_DELIVERY", // 착불
}
```

- [x] CartProduct

```typescript
interface CartProduct {
    // ─────────────────────────────────────
    // 식별
    // ─────────────────────────────────────
    productNo: number; // 상품번호
    productName: string; // 상품명
    imageUrl: string; // 상품 이미지

    // ─────────────────────────────────────
    // 파트너
    // ─────────────────────────────────────
    partnerNo: number; // 파트너번호
    partnerName: string; // 파트너명

    // ─────────────────────────────────────
    // 구매 제한
    // ─────────────────────────────────────
    minBuyCount: number; // 최소 구매수량
    maxBuyPersonCount: number | null; // 1인당 최대 구매수량
    maxBuyTimeCount: number | null; // 1회당 최대 구매수량

    // ─────────────────────────────────────
    // 상태
    // ─────────────────────────────────────
    liked: boolean; // 찜 여부
    optionUsed: boolean; // 옵션 사용 여부
    couponUsable: boolean; // 쿠폰 사용 가능 여부
    accumulationUsable: boolean; // 적립금 사용 가능 여부
}
```

- [x] CartOption

```typescript
interface CartOption {
    // ─────────────────────────────────────
    // 식별
    // ─────────────────────────────────────
    cartNo: number; // 장바구니 번호 (PK)
    productNo: number; // 상품번호
    optionNo: number; // 옵션번호

    // ─────────────────────────────────────
    // 옵션 정보
    // ─────────────────────────────────────
    optionName: string; // 옵션명
    optionValue: string; // 옵션값
    optionTitle: string; // 옵션 표시값 ("색상: 빨강 / 사이즈: M")
    optionType: OptionType; // 옵션 타입
    isRequiredOption: boolean; // 필수 옵션 여부
    isExtraProduct: boolean; // 추가상품 여부

    // ─────────────────────────────────────
    // 수량/가격
    // ─────────────────────────────────────
    orderCnt: number; // 주문수량
    salePrice: number; // 판매가
    addPrice: number; // 옵션 추가금액
    buyAmt: number; // 구매금액 (구매가 * 수량)
    immediateDiscountAmt: number; // 즉시할인금액

    // ─────────────────────────────────────
    // 재고
    // ─────────────────────────────────────
    stockCnt: number; // 재고수량
    soldOut: boolean; // 품절 여부

    // ─────────────────────────────────────
    // 이미지
    // ─────────────────────────────────────
    imageUrl: string; // 옵션 이미지

    // ─────────────────────────────────────
    // 유효성
    // ─────────────────────────────────────
    valid: boolean; // 유효 여부
    validErrorCode: string | null; // 유효성 실패 코드
    validMessage: string | null; // 유효성 실패 메시지
    orderCntChangeable: boolean; // 수량 변경 가능 여부

    // ─────────────────────────────────────
    // 예약주문
    // ─────────────────────────────────────
    reservation: boolean; // 예약주문 여부
    reservationDeliveryYmdt: string | null; // 예약주문 배송시작예정일
}

enum OptionType {
    PRODUCT_ONLY = "PRODUCT_ONLY", // 상품단독
    OPTION_ONLY = "OPTION_ONLY", // 옵션단독
    COMBINATION = "COMBINATION", // 조합형
}
```

- [x] CartSetOption

```typescript
interface CartSetOption {
    // ─────────────────────────────────────
    // 식별
    // ─────────────────────────────────────
    mallOptionNo: number; // 옵션번호
    mallProductNo: number; // 상품번호
    stockNo: number; // 재고번호

    // ─────────────────────────────────────
    // 옵션 정보
    // ─────────────────────────────────────
    optionName: string; // 옵션명
    optionValue: string; // 옵션값
    optionPrice: number; // 옵션가격
    count: number; // 수량
    usesOption: boolean; // 옵션사용여부
    productName: string; // 상품명
}
```

## 작업 내용
